#ifndef INDICE
#define INDICE

#include <string>
#include <vector>
#include <list>
#include <algorithm>
#include <set>

using namespace std;

struct indice {
    string word;
    int reps;
    float percentage(int);
};

#endif